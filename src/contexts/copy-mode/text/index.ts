/**
 * Text extraction module for copy mode
 */

export {
  TextExtractionError,
  createLineAccessor,
  extractBlockText,
  extractRangeText,
  extractLineAtCursor,
  prepareCopyText,
  type CopyResult,
} from './extraction';
