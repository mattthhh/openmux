/**
 * Tree types for aggregate view (re-exports from main types for convenience).
 */

export type {
  SessionLoadState,
  SessionTreeNode,
  PtyTreeNode,
  PlaceholderTreeNode,
  SpacerTreeNode,
  TreeNode,
  FlattenedTreeItem,
  TREE_GLYPHS,
} from '../types';
